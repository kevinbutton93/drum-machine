import React from "react"

const Button = (props) => {
    const buttonClass = "drum-pad btn btn-warning "

    return (
        
            <button 
                
                type="button" 
                className={buttonClass}
            >
            </button>
        
    )
}

export default Button